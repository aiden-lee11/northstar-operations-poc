# Old refunds dashboard

An earlier read-only refunds view I built before narrowing the demo to feature flags. Kept for reference only.

This isn't part of the app and doesn't run on its own. It's excluded from the build, the tests, and anything the server will serve. Some imports in here point at files that have since moved.

All the data was fake. Nothing ever moved money.
